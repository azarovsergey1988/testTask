{
  "scripts": {
    "prerunTests": "selenium-standalone install",
    "runServer": "selenium-standalone start",
    "test": "codeceptjs run --steps",
    "runTests": "npm-run-all -p -r runServer test"
  },
  "name": "testTask",
  "version": "0.0.1",
  "dependencies": {},
  "devDependencies": {
    "npm-run-all": "^4.1.3"
  }
}
